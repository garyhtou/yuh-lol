<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />

		<!-- SEO/OG Meta -->
		<title><%= title %></title>
		<meta property="og:title" content="<%= title %>" />
		<meta property="og:url" content="<%= domain %> " />
		<meta property="og:type" content="website" />
		<meta property="og:description" content="<%= gif.title %>" />
		<meta property="og:image" content="<%= url %> " />

		<meta name="twitter:card" content="summary_large_image" />

		<!-- Styles and Fonts -->
		<link rel="stylesheet" href="/styles.css" />
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link
			href="https://fonts.googleapis.com/css2?family=Corinthia:wght@700&display=swap"
			rel="stylesheet"
		/>
	</head>
	<body>
		<div class="container">
			<h1 class="title">yuhhh</h1>
			<div class="img-wrapper">
				<img src="<%- url %>" alt="<%= gif.title %>" />
			</div>
		</div>
		<script nonce="<%= nonce %>">
			(() => {
				const audio = new Audio('<%- music %>');
				audio.loop = true;
				audio.volume = 0.5;
				audio.play();

				document.body.addEventListener('click', function () {
					if (audio.paused) {
						audio.play();
					}
				});

				// Reload window after 59 minutes to refresh audio link
				setTimeout(() => {
					location.reload();
				}, 1000 * 60 * 59);
			})();
		</script>
	</body>
</html>
